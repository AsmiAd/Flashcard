import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF6C9DFE); // soft blue
  static const secondary = Color(0xFFB3C7FF);
  static const background = Color(0xFFF7FAFF);
  static const text = Color(0xFF1E1E1E);
  static const grey = Color(0xFF9E9E9E);
  static const white = Colors.white;
  static const error = Color(0xFFE53935);

  // Dark Theme
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E); // For input fields
  static const darkText = Color(0xFFE0E0E0);
}
